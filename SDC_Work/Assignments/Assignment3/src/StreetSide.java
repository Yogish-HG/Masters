/**
 * Create a type to know which side of a street we lie on.  The side assumes that the street
 * has an orientation, given when the street is created.
 */
public enum StreetSide {
    Left, Right;
}
